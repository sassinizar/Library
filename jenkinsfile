pipeline {

    agent any;

    stages {

        stage("build") {
            steps {
               sh 'echo build stage'     
            }
        }
        stage("push") {
            steps {
                sh 'echo push'
            }
        }
        stage("deploy") {
            steps {
                sh 'echo deploy'
            }
        }
    }
}