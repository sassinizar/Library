pipeline {

    agent any;

    stages {

        stage('build') {
            steps {
               echo 'build stage....'     
            }
        }
        stage('push') {
            steps {
                echo "the scond stage push..."
            }
        }
        stage('deploy') {
            steps {
                echo "the third stage deploy...."
            }
        }
    }
}
